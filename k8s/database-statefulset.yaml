apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: nagp-app
  labels:
    app: postgres
spec:
  serviceName: postgres-service
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      name: postgres
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
          name: postgres
        env:
        - name: POSTGRES_DB_FILE
          value: /etc/secrets/POSTGRES_DB
        - name: POSTGRES_USER_FILE
          value: /etc/secrets/POSTGRES_USER
        - name: POSTGRES_PASSWORD_FILE
          value: /etc/secrets/POSTGRES_PASSWORD
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
        - name: init-script
          mountPath: /docker-entrypoint-initdb.d
        - name: db-secret-volume
          mountPath: /etc/secrets
          readOnly: true
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - admin
            - -d
            - employeedb
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command:
             - pg_isready
             - -U
             - admin
             - -d
             - employeedb
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
      - name: postgres-storage
        persistentVolumeClaim:
          claimName: postgres-pvc
      - name: init-script
        configMap:
          name: postgres-init-script
      - name: db-secret-volume
        secret:
          secretName: db-secret
          items:
          - key: POSTGRES_DB
            path: POSTGRES_DB
          - key: POSTGRES_USER
            path: POSTGRES_USER
          - key: POSTGRES_PASSWORD
            path: POSTGRES_PASSWORD
